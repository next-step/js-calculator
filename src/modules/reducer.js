const initialState = {
  display: '',
  digitCount: 0,
}

const reducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return {
        ...state,
      }
  }
}

export default reducer
