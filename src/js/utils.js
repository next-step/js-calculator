export const isEmpty = (target) => target === "";

export const isEqual = (target, compare) => target === compare;

export const isSame = (a, b) => a === b;

export const isGreaterThan = (a, b) => a > b;

export const isSmallerThan = (a, b) => a < b;
