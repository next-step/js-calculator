export const Operator = {
  PLUS: '+',
  MINUS: '-',
  MULTIPLE: 'X',
  DIVISION: '/',
  EQUALS: '=',
};

export const MAX_VALUE = 999;
export const INITIAL_NUMBER = '0';

// export const operators = new Set([Operator.PLUS, Operator.MINUS, Operator.MULTIPLE, Ope, 5]);
