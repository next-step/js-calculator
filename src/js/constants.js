const MAX_INPUT_NUMBER_LENGTH = 3;

const SELECTORS_CLASS = {
  DIGIT: '.digit',
  MODIFIER: '.modifier',
  OPERATION: '.operation',
  OPERATIONS: '.operations',
};

const SELECTORS_ID = {
  TOTAL: '#total',
}

export {
  MAX_INPUT_NUMBER_LENGTH,
  SELECTORS_CLASS,
  SELECTORS_ID,
}