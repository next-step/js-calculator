function sum(a, b) {
  return a + b;
}

function subtract(a, b) {
  return a - b;
}

function multiple(a, b) {
  return a * b;
}

function divide(a, b) {
  return Math.floor(a / b);
}

module.exports = {
  sum,
  subtract,
  multiple,
  divide,
};
