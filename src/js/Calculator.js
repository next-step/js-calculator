class Calculator {
  add(n, m) {
    return n + m;
  }
  subtract(n, m) {
    return n + m; // TODO:
  }
  multiply(n, m) {
    return n + m; // TODO:
  }
  divide(n, m) {
    return n + m; // TODO:
  }
}

export default Calculator;
