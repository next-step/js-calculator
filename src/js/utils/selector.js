export const SELECTORS = {
  ID: {
    TOTAL: '#total',
  },
  CLASS: {
    DIGITS: '.digits',
    MODIFIER: '.modifier',
    OPERATIONS: '.operations',
  },
};

export const $ = (selector) => document.querySelector(selector);
