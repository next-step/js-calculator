export default class Calculate {
 plus(num1, num2) {
  return num1 + num2;
 }

 minus(num1, num2) {
  return num1 - num2;
 }
 multiple(num1, num2) {
  return num1 * num2;
 }
 divide(num1, num2) {
  return num1 / num2;
 }
}
